<!-- 
目的：「プロジェクトの存在理由」の明文化
 -->

# SPEC_OVERVIEW.md (プロジェクトの存在理由)

> 各プラグインリポジトリ直下の `docs/` フォルダ等にコピーして利用することを想定しています。
> ファイル名の一例: `docs/SPEC_OVERVIEW.md`

* **目的**:
  * プラグインの「存在理由」「解決したいユーザー課題」「スコープ/非スコープ」を明文化する。
* **AI 的メリット**:
  * 「このプラグインは何のためのものか ?」を毎回説明せずに済む。
  * 仕様追加時に「スコープ外」を AI に判定させやすくなる。

---

## 1. プラグインの目的 (Why)

* プラグイン名:
* タグライン (1行で要約):
* このプラグインが解決したいユーザー課題:

```text
* このプラグインが解決したいユーザー課題:
  * 中小企業の〇〇担当者が、△△作業にかける時間を50%削減する
  * 投稿者が□□を意識せずに SEO に強い記事構造を作れるようにする
```

### 1.1. ターゲットユーザー

* 代表的なペルソナ:

```text
* 代表的なペルソナ:
  * 役割:
  * スキルレベル (WordPress 経験、技術レベル等):
  * 典型的な利用シナリオ:
```

### 1.2. 提供しないもの (非スコープ)

* 明示的に「やらないこと」「対応しないケース」を列挙してください。

```text
* 非スコープ:
  * EC 機能 (カート/決済) は扱わない
  * マルチサイト環境は初期バージョンではサポート外
```

---

## 2. 成果物と成功指標 (What)

### 2.1. 主な機能一覧

```text
* 機能1:
* 機能2:
* 機能3:
```

### 2.2. 成功指標 (KPI/採用基準)

* 導入サイト数や有効インストール数など、可能であれば数値目標を書く
* ユーザー体験の指標 (例: 設定完了までのクリック数、1画面あたりの入力項目数)

---

## 3. 想定環境・前提条件

* 本章では、「開発に必要な技術情報」を記載します。

* 対応 WordPress バージョン: たとえば、「WordPress 6.x 系を対象」
* 対応 PHP バージョン:
* 対応 Node.js バージョン: たとえば、「v18以降」
* 対応 npm バージョン: たとえば、「v9以降」
* ローカル開発は「Local by Flywheel」または同等の環境を利用すること
* 必須となる他プラグイン/テーマとの依存関係: あれば明記

### 3.1. フロントエンド技術スタック

* **React v18.2**:
  * 管理画面 UI の構築
* **TypeScript v5.9**:
  * 型安全性の確保
* **SCSS**:
  * スタイル管理とデザインシステム
* **Vite v7.1**:
  * 高速ビルドとモジュールバンドリング

### 3.2. ビルド要件

* Vite + TypeScript + SCSS
  * `vite.config.ts` を用いて IIFE 形式でバンドルする
  * JavaScript は WordPress 同梱の jQuery を利用可能とする (外部 import 不要) (`jQuery(function($) { ... })`)
  * CSS も IIFE 出力し、エディター用・フロント用を区別すること
* 出力は `./dist`

### 3.3. `package.json` の `scripts`

* `npm run build:dev` → 開発用ビルド (minify 無効)
* `npm run build:production` → 本番用ビルド (minify 有効)
* `npm run dev` → 開発用ビルド (watch モード)
* `npm run lint` → ESLint + Stylelint によるコード品質チェック
* `npm run makepot` → 翻訳テンプレート生成

---

## 4. システム境界 (他システム/外部サービスとの関係)

* 連携する外部サービス:
* 取得・送信するデータ概要:
* 認証・認可方式 (API キー、OAuth など):

---

## 5. AI 伴走開発の観点メモ (任意)

* このプラグインで AI に特に気を付けてほしい点:
  * 例: 「文言は必ず i18n 関数を通すこと」「セキュリティチェックを絶対にスキップしないこと」
* AI に渡すときに必ず一緒に読ませたいドキュメント:
  * `WP_PLUGIN_SPEC.md`
  * `SPEC_ARCHITECTURE.md`
  * `SPEC_DATA_DICTIONARY.md` など
